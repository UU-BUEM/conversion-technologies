# docs

- [`calliope_schema_mapping.md`](calliope_schema_mapping.md) — how the
  internal `TechnologySpec` fields map to each supported Calliope YAML
  schema, and the known limitations of the `modern` exporter.

See also the root [`CLAUDE.md`](../CLAUDE.md) for architecture/data-flow and
[`.claude/`](../.claude/README.md) for open items and past decisions.
